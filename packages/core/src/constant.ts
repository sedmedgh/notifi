export enum CONSTANTS {
  // Visible notifications amount
  VISIBLE_NOTIFICATIONS = 5,
  // Viewport padding
  VIEWPORT_OFFSET = 32,
  // Mobile viewport padding
  MOBILE_VIEWPORT_OFFSET = 16,
  // Default lifetime of a notifications (in ms)
  NOTIFICATION_DURATION = 4000,
  // Default notification width
  NOTIFICATION_WIDTH = 448,
  // Default gap between notifications
  GAP = 8,
  // Threshold to dismiss a notification
  SWIPE_THRESHOLD = 45,
  // Equal to exit animation duration
  TIME_BEFORE_UNMOUNT = 200,
  MAX_SAVED_DISMISSED_NOTIFICATION = 100,
}
